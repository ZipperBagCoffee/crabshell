---
description: Clear old session memories (keeps recent ones)
allowed-tools: Bash
---

Invoke the clear-memory skill to archive or clear old session memories ($ARGUMENTS: "old" = compress 30+ day files (default), "all" = delete all session files after confirmation).
